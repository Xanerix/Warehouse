buildscript {
    repositories {
        google()
        jcenter() // 可选，留着也行
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.8.0'
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}
